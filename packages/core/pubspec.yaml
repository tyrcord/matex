name: matex_core
description: The core tool of the Matex packages.
version: 0.0.13
repository: https://github.com/tyrcord/matex/tree/main/packages/core

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter

  # Public
  decimal: ^2.3.3
  collection: ^1.17.2
  share_plus: ^7.1.0
  intl: ^0.18.0
  easy_localization: ^3.0.3

  # T
  tstore: ^0.8.15
  tbloc: ^0.21.4
  t_helpers: ^0.0.29
  tmodel: ^0.5.8

  # Lingua
  lingua_core: ^0.0.23
  lingua_calculator: ^0.0.3

  # Fastyle
  fastyle_settings: ^0.0.25
  fastyle_core: ^0.0.20
  fastyle_calculator: ^1.30.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
  import_sorter: ^4.6.0
  dependency_validator: ^3.2.2
